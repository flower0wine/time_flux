# 📱 广告跳过功能使用指南

## 功能介绍

Time Flux 现在支持自动跳过应用开屏广告功能，通过 Android 无障碍服务实现智能识别和自动点击"跳过"按钮。

## ✨ 核心特性

- ✅ **智能识别**: 自动识别"跳过"、"关闭"、"×"等常见广告按钮
- ✅ **规则配置**: 支持自定义跳过规则，可针对特定应用设置
- ✅ **跳过记录**: 记录每次跳过的详细信息，可查看历史记录
- ✅ **无需 Root**: 基于无障碍服务，无需 Root 权限
- ✅ **隐私安全**: 所有数据本地存储，不上传任何信息

## 🚀 使用步骤

### 1. 启用无障碍服务

1. 打开 Time Flux 应用
2. 切换到"广告跳过"标签页
3. 点击"打开无障碍设置"按钮
4. 在系统设置中找到"Time Flux"服务
5. 开启服务权限

### 2. 查看跳过记录

- 切换到"跳过记录"标签页
- 查看已跳过的广告列表
- 包含应用名称、关键词、时间等信息
- 可以清空历史记录

### 3. 管理跳过规则

#### 默认规则

应用内置了两条默认规则：

1. **通用跳过**: 识别"跳过"、"skip"、"Skip"、"SKIP"、"关闭"、"close"
2. **通用关闭**: 识别"×"、"✕"、"关闭广告"、"Close Ad"

#### 添加自定义规则

1. 切换到"规则管理"标签页
2. 点击"添加规则"按钮
3. 填写规则信息：
   - **规则名称**: 给规则起个名字
   - **应用包名**: (可选) 只对特定应用生效，留空则全局生效
   - **关键词**: 用逗号分隔多个关键词，如 "跳过,关闭,skip"
4. 点击"保存"

#### 管理现有规则

- 使用开关启用/禁用规则
- 点击规则可以查看详情
- 长按可以删除规则

## 🔧 技术实现

### 架构设计

```
┌─────────────────────────────────────┐
│   AdSkipAccessibilityService        │  无障碍服务
│   - 监听屏幕内容变化                 │
│   - 识别广告按钮                     │
│   - 执行点击操作                     │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│   AdSkipManager                      │  规则管理器
│   - 规则存储与管理                   │
│   - 跳过记录保存                     │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│   AdSkipScreen (Compose UI)          │  用户界面
│   - 服务状态显示                     │
│   - 记录查看                         │
│   - 规则配置                         │
└─────────────────────────────────────┘
```

### 核心组件

1. **AdSkipAccessibilityService**: 无障碍服务，监听屏幕内容并执行跳过操作
2. **AdSkipManager**: 管理跳过规则和记录
3. **AdSkipRule**: 规则数据模型
4. **SkipRecord**: 跳过记录数据模型
5. **AdSkipScreen**: Compose UI 界面

### 识别策略

服务使用多种策略识别广告按钮：

1. **文本匹配**: 通过 `findAccessibilityNodeInfosByText()` 查找包含关键词的节点
2. **ViewId 匹配**: 识别包含 "skip"、"close"、"dismiss" 等关键词的 ViewId
3. **层级遍历**: 如果节点本身不可点击，尝试点击父节点
4. **坐标点击**: 如果节点不响应点击事件，使用手势模拟点击坐标

### 点击执行

1. 优先使用 `performAction(ACTION_CLICK)`
2. 如果失败，尝试点击父节点
3. 最后使用 `dispatchGesture()` 模拟坐标点击

### 防重复机制

- 设置 1 秒冷却时间，避免重复点击
- 记录最后跳过时间，在冷却期内不处理事件

## 📝 代码文件说明

### 共享模块 (shared)

- `AdSkipRule.kt`: 规则和记录的数据模型定义
- `AdSkipManager.android.kt`: Android 平台的规则管理实现

### Android 应用 (androidApp)

- `AdSkipAccessibilityService.kt`: 无障碍服务核心实现
- `AdSkipScreen.kt`: Compose UI 界面
- `MainActivity.kt`: 主界面，集成广告跳过和壁纸设置功能
- `accessibility_service_config.xml`: 无障碍服务配置
- `strings.xml`: 字符串资源

## ⚠️ 注意事项

1. **权限要求**: 需要开启无障碍服务权限
2. **电池优化**: 建议将应用加入电池优化白名单，避免服务被杀
3. **兼容性**: 不同应用的广告实现方式不同，可能需要添加自定义规则
4. **隐私**: 无障碍服务可以读取屏幕内容，请确保从官方渠道下载应用

## 🔍 常见问题

### Q: 为什么有些广告跳过不了？

A: 可能原因：
- 广告按钮使用了图片而非文字
- 按钮文字不在默认关键词列表中
- 应用使用了特殊的广告展示方式

解决方法：添加自定义规则，针对特定应用配置关键词

### Q: 服务会不会影响性能？

A: 服务只在屏幕内容变化时触发，且有冷却机制，对性能影响很小

### Q: 数据存储在哪里？

A: 所有数据（规则和记录）存储在应用的 SharedPreferences 中，不会上传到服务器

### Q: 如何验证服务是否正常运行？

A: 在"服务状态"标签页查看服务状态，如果显示"服务已启用"则正常运行

## 🛠️ 开发与调试

### 查看日志

```bash
adb logcat | grep AdSkipService
```

### 测试服务

1. 启用无障碍服务
2. 打开任意有开屏广告的应用
3. 观察是否自动跳过
4. 查看"跳过记录"标签页确认

### 添加新规则

在 `AndroidAdSkipManager.kt` 的 `DEFAULT_RULES` 中添加默认规则：

```kotlin
AdSkipRule(
    id = "custom_rule",
    name = "自定义规则",
    packageName = "com.example.app", // 可选
    keywords = listOf("跳过", "skip")
)
```

## 📄 许可证

本功能遵循项目整体许可证。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进广告跳过功能！
